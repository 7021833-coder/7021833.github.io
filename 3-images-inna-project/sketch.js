// Image Demo

let monkeyImg;

function setup() {
  createCanvas(windowWidth, windowHeight);
  monkeyImg= loadImage("Monkey-Selfie.webp")
}
function setup(){
createCanvas(windowWidth, windowHeight;)
imageMode(CENTER)
}

function draw() {
  background(220);
  image (monkeyImg, mouseX, mouseY, monkeyImg.width*0.5, monkeyImg.height*0.5);
}
